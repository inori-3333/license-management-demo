# 交互约定

依据：用户已确认实施方案；sources 中任务书与指导意见为业务证据。正文与附件冲突按用户确认的正文优先口径演示。

## Canonical UI Map

| Capability      | Canonical owner                 | Source of truth | Allowed variants                 | Verification     |
| --------------- | ------------------------------- | --------------- | -------------------------------- | ---------------- |
| Table Selection | Talent selected state           | 本文            | 显式人员选择，导出只取筛选内已选 | docs/验收记录.md |
| Select/Listbox  | src/ui.tsx Select               | 本文            | native，接受操作系统弹出菜单     | keyboard         |
| Date            | src/ui.tsx Field                | 本文            | native date，接受浏览器日历      | docs/验收记录.md |
| Form            | src/ui.tsx BusinessForm + Field | 本文            | create/edit                      | docs/验收记录.md |
| Scrollbar       | src/styles.css                  | DESIGN.md       | 全局样式，表格独立滚动           | docs/验收记录.md |
| Toast           | src/store.tsx Provider          | 本文            | 全站唯一 live region             | docs/验收记录.md |
| CRUD            | pages + store.update            | 实施方案        | 编辑返回详情／列表，保持筛选     | docs/验收记录.md |

新增共用组件：`src/components/SearchPicker.tsx` 负责大量岗位选项的搜索与专业分组，采用原生 dialog、搜索框、原生专业筛选和按钮列表；不与 Select 的原生单选菜单混用。`src/components/RuleConditions.tsx` 负责只读规则条件，组内“或”、组间“且”，折叠内容保留全部条件。

## 共同操作

列表的搜索、筛选、排序与页码保存在 Hash URL 查询参数；报表编辑条件保存在组件状态，用户明确保存后进入本地报表配置。变更筛选回到第一页。表格分页 10/20/50，列排序及空结果状态统一。选择人物后导出的人员范围仅限当前筛选内已选人员；未选择时为全部筛选结果。

人员详情返回台账时恢复原筛选、排序、分页及页面／表格滚动位置；直接打开详情时返回默认台账。搜索框在中文输入组合期间保留输入草稿，选词完成后再筛选，清空后焦点回到搜索框。

台账、整改、报表在 640px 及以下使用摘要卡片，排序、分页与桌面表格共用状态；宽表保留身份列、操作列，只有发生横向溢出才显示滚动提示。窄屏通过带文字的导航菜单访问所有页面。舒适表格默认 14px，紧凑表格为 13px，字号与间距同步变化，仅密度偏好保存在独立本地键中。

首页先显示可跳转处理的待办，单位列表默认展示六家并允许排序／展开全部。30 天临期入口与报表时间筛选采用同一时间边界（包括当天和第 30 天，不包含已逾期）。单位报表默认五个核心指标，可以显示全部指标；行明细始终展示完整数据及分子分母。显示列同时决定导出列，旧保存方案保持原有选列语义。

岗位选择弹窗支持关键词、专业筛选、分组、分批展开、选中状态及空结果；点击岗位立即确认，取消或 Escape 保留原选择并回焦到触发按钮。整改弹窗标题始终包含对象名称，表单之前显示工号、单位、当前岗位、处理期限，便于核对对象。

表单草稿在弹窗中编辑，保存后关闭并显示成功信息，取消关闭并丢弃草稿；采用单管理员 Demo 简化流程，不做离开确认和多用户冲突处理。删除证书、重置及恢复备份需应用内确认。Modal 使用 dialog.showModal 提供背景 inert、焦点限制和 Escape；关闭后恢复触发控件焦点。

本系统为用户明确限定的浏览器本地 Demo，无远程分页、重试、离线同步、认证或服务端授权。全部数据更新为本地同步计算。仅保留任务必需的输入与业务校验。

## 业务行为

原始四列与行号不覆盖，源数据班组为空允许直接归属部门，岗位为空进入待补充。姓名与持证为模拟补充字段。名称推荐不自动采纳。

整改顺序：待分派 → 待整改 → 整改中 → 待复核 → 已销项；复核可退回整改中。复核销项前重新执行当前规则，存在原问题则阻止销项。负责人和复核人以文字录入模拟职责，不模拟账户授权。

zh-CN 中文应用；日期为无时区日期字符串，倒计时按 UTC 日序差计算；处理时间线按 Asia/Shanghai 显示。非业务日志不进入产品页面。

## 验证

npm test；npm run verify:data；npm run build；npm run verify:ui；设计技能静态审计。浏览器验证由真实 Chromium 完成，包括业务闭环、数据筛选导出、焦点与窄屏。无后端服务的网络状态测试不适用。

## 自动演示

依据：用户要求减少 75 项零散演示，避免视线刚转移就跳页，并通过 KTV 式逐字变色让观众预期下一条的开始时间。现合并为 8 个模块、16 段业务流程，移除同类报表维度等重复轮播。

App 保持原有导航、页面及表单弹窗；AutoDemo 不创建 iframe、新页面或整页演示弹窗。Provider 开始时为当前界面提供种子数据的内存副本，原工作数据保留不动。演示结束或中断后恢复原 URL、统计数据和表格密度，内存副本不写入 localStorage。重播重新创建演示数据。

src/demo/driver.ts 通过原有菜单、链接、筛选器和表单连续完成一段业务操作。结果目标就位后先稳定 1 秒，再逐字变色讲解（每字 180 毫秒，至少 4.2 秒），读完保留 1.2 秒，最后进入下一段。讲解期间不执行新的鼠标移动、滚动、填表或路由切换。卡片预告下一段，字幕不重复播报给读屏软件。操作提示仅在移动、点击、输入或选择选项时显示，限制在两行内；阅读、暂停、等待和完成时留空，不显示状态说明或倒计时。预留 40px 紧凑空间以保持卡片稳定，空闲时隐藏背景和标记。

移动、点击、输入、稳定等待、逐字变色和末尾停留共用可暂停时钟，倍速同时影响操作与字幕。下一条只跳过已完成操作的讲解，不跳过业务操作。切到后台或按 Escape 暂停。可见控件不反复滚动居中，页面或业务弹窗切换后留出视线转移时间。

讲解卡片在同一业务区域内优先保留原位置，只有遮挡目标、展开选项、越出视口或进入新区时重新定位。原业务弹窗打开时，演示层移入该弹窗的手动 Popover 中，保证可见且播放控制可用。鼠标为明显标识的演示指针；点击有可见圆环反馈。正常使用保持原生选择器，自动演示以只读选项预览展示选择过程，再写入原选择器并触发原有逻辑。文件选择展示示例文件名称并走真实解析流程。

退出立即取消鼠标移动、打字、等待和未完成的演示步骤；恢复启动入口焦点。演示中业务控件由脚本操作，观看者可以滚动和使用讲解卡片中的暂停、继续及退出按钮。Excel、CSV、JSON 和框架方案保存在本次演示文件列表；打印展示页面内真实报表版式，避免打开系统对话框阻断自动播放。

验证：npm run test:demo 覆盖原界面无 iframe、完整业务动作、导出内容、数据恢复、鼠标轨迹、点击位置、输入节奏、暂停冻结、逐字变色、读完后停留、阅读期间页面和焦点不变、选项可见性和卡片布局。浏览器时钟加速用于完整流程回归，另以正常速度验证实际观看表现。

16 段业务操作与最后一段讲解全部结束后，在原应用主内容区自动进入全流程导图，移除演示指针与聚焦框，保留导航外壳。导图按业务关系组合全部 16 个环节，编号仍对应原演示顺序。六个阶段节点可用鼠标、触摸和键盘选择，图下展示该阶段的原讲解；设置、备份和演示重置作为支撑能力单列。标题接收焦点，页面回到顶部；之后不自动移动视口、不自动切换节点。连线动效可暂停／继续，Escape 和后台切换暂停，动态响应系统减少动态效果偏好。窄屏沿业务顺序纵向排列，所选阶段的讲解在节点内展开，避免跳至页面底部。下载列表、重新演示和结束后恢复原页面、原数据的契约保持一致。
