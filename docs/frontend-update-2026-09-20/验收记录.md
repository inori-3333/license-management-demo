# 七项前端优化验收 · 2026-09-20

本轮按已确认顺序完成七项修改，沿用原有蓝白视觉、浏览器本地数据和业务规则。预览地址：<http://127.0.0.1:5173/#/>。

## 修改结果

| 顺序 | 修改         | 当前行为                                                                                                 |
| ---- | ------------ | -------------------------------------------------------------------------------------------------------- |
| 1    | 首页聚焦行动 | 合规问题、判定信息待补充、30 天提醒前置；单位默认六家，支持三种排序及展开全部；公司筛选同步带入下钻。    |
| 2    | 表格与窄屏   | 桌面固定关键列并按实际溢出显示滚动提示；640px 以下台账、整改、报表改用摘要卡片，共用排序和分页。         |
| 3    | 整改上下文   | 标题显示人员姓名，表单前显示工号、单位、岗位、处理期限和倒计时；原有处理步骤、当前校验与时间线保留。     |
| 4    | 岗位选择     | 628 个岗位可按名称／专业搜索、按专业筛选、分组查看和分批展开；支持空结果、选中状态、键盘确认、取消保留。 |
| 5    | 规则可读性   | 条件分为专业、岗位、职责、实际作业；组内或、组间且，全部条件与依据可展开；口径说明和类别维护默认折叠。   |
| 6    | 报表层次     | 单位报表默认五个核心指标，一键显示全部 11 项；行明细展示完整数据与分子分母，并可进入单位人员台账。       |
| 7    | 字体与导航   | 表格默认 14px，紧凑模式 13px，辅助文字至少 12px；密度偏好独立保存，窄屏有完整文字导航，跨页面进入页首。  |

同时修正：台账表头改为“已满足／应满足”；未知作业范围显示“作业范围待确认”；人员详情返回保留原筛选、排序、分页及滚动位置。搜索支持输入组合期间保留草稿，岗位弹窗打开即聚焦搜索框。

## 本轮浏览器验证

使用真实 Chromium 预览，检查默认 684px、1280×900 与 390×844；已重置临时视口。

- 首页：默认六家、展开 15 家、切换待确认人数排序；全部公司 30 天提醒为 170 项，A 公司为 19 项，进入报表后数量与筛选一致。公司范围内的人数、岗位来源数及下钻参数一致。
- 台账：SCENE003 → 郑华详情显示作业范围待确认 → 页内返回保留搜索。A 公司、姓名升序、每页 10 条、第 2 页 → 详情 → 返回，查询条件完整保留；一次记录页面滚动为 259.5px，返回仍为 259.5px；横向滚动 80px 也恢复。无结果与清空恢复正常。
- 整改：首条弹窗明确显示郑彤、DEMO0131、E 公司／汽机车间、综合班主行车工及 2026-09-13 今天到期；桌面、手机均可阅读；本轮未提交分派或销项。
- 岗位：初始 628 项、首批 40 项、展开至 80 项；不存在关键词显示 0 项；清空回焦；电气检修专业搜索“技术员”出现两个岗位；Enter 选择“电气检修技术员”后匹配结果更新；重新打开并 Escape 保留原选择、焦点回到触发按钮。打开即聚焦搜索框。
- 规则：展开高压电工作业证专业组后显示电气检修、运行、新能源、储能四项，且连接实际作业条件；依据完整保留。
- 报表：默认五项数据列与明细操作，切换全部指标为 11 项数据列；A 公司明细显示 9/11 人、329/386 项及统计范围；Escape 回焦明细按钮。手机可以打开同一完整明细。
- 密度：舒适表格 14px → 紧凑 13px，刷新后仍为紧凑；已恢复舒适模式。
- 手机：台账、整改、报表卡片和八项文字导航可用；台账和整改页面实际宽度均为 390px，没有整页横向溢出；岗位选择弹窗宽 374px，取消按钮在视口内。
- 运行时：本轮浏览器控制台读取到的 error 记录为空。

未宣称覆盖真实手机、所有浏览器、读屏器或完整 WCAG 审计。中文文本搜索与清空已验证，真实输入法选词过程未通过自动化重现。本轮没有重新走完整导入、证书增删或整改销项；这些业务未被本轮改写。

## 自动检查

- `npm test`：56 项通过，新增 30 天提醒的当天、第 30 天、第 31 天、逾期和公司范围边界验证。
- `npm run verify:data`：原表 1,315 行逐字段一致，15 家公司，124 处班组空白，4 处岗位空白。
- `npm run build`：TypeScript 与 Vite 生产构建通过。
- `npm run verify:pages`：7 个生产文件在子路径下返回正常，Hash 路由资源完整。
- `npm run verify:ui`：设计 tokens 与文本对比度检查通过。
- `npm run format:check`：格式检查通过。
- 严格设计静态审计：0 errors、0 warnings、0 violations；结果见同目录 `premium-audit.json`。该结果仅代表脚本范围内的静态检查。
- `designmd lint DESIGN.md`：0 errors、0 warnings；提示未定义 spacing 节点，现有间距继续由共享 CSS 维护。

## 截图

![首页宽屏](/Users/inori333/code_/chd-license-system/docs/frontend-update-2026-09-20/01-overview-desktop.png)

![手机台账](/Users/inori333/code_/chd-license-system/docs/frontend-update-2026-09-20/02-people-mobile.png)

![整改上下文](/Users/inori333/code_/chd-license-system/docs/frontend-update-2026-09-20/03-remediation-context.png)

![岗位搜索](/Users/inori333/code_/chd-license-system/docs/frontend-update-2026-09-20/04-job-picker.png)

![规则条件](/Users/inori333/code_/chd-license-system/docs/frontend-update-2026-09-20/05-rule-conditions.png)

![统计明细](/Users/inori333/code_/chd-license-system/docs/frontend-update-2026-09-20/06-report-detail.png)

![文字导航](/Users/inori333/code_/chd-license-system/docs/frontend-update-2026-09-20/07-mobile-navigation.png)

![手机整改](/Users/inori333/code_/chd-license-system/docs/frontend-update-2026-09-20/08-issues-mobile.png)

![手机岗位选择](/Users/inori333/code_/chd-license-system/docs/frontend-update-2026-09-20/09-job-picker-mobile.png)

![报表概览](/Users/inori333/code_/chd-license-system/docs/frontend-update-2026-09-20/10-report-overview-desktop.png)
