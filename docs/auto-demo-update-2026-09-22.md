# 自动演示更新验收（2026-09-22）

## 更新内容

自动演示沿用当前页面操作、临时数据、同步逐字讲解及 1.2 秒结果停留，随最新功能扩展为 9 个模块、19 段业务流程。

- 新增“按岗位与持证条件筛选”：A 公司 → 当前岗位“检修工” → 同时持有生产岗位能力认证证书、电力安全技能认证证书 → 查看筛选结果。
- “岗位匹配与培训需求”承接上述人选，明确选择“储能运维工程师”，以 2027-01-01 评估资格，导出所选人员的缺口，随后演示清空条件、无结果及恢复列表。
- 岗位选择不再依赖搜索结果第一项，避免把同专业的其他岗位误当成目标岗位。
- 全流程导图包含新增环节，知识图谱为第 16、17 段，设置与备份编号更新为 18、19，回顾说明与演示同步。
- 接入本地新增知识图谱：浏览六类节点全景、缩放与居中，按人员类型和 SCENE001 工号定位陈明，查看任职与有效高压电工作业证关联。
- 继续打开人员详情，按证书类型查询高压电工作业证，沿关联进入持证规则及业务页面，最后验证无结果、清除筛选、回到全景。
- 画布使用完整避让范围；手机人员定位结束时聚焦具体持证记录，避免讲解卡片遮住当前结果。
- 回归测试等待最新品牌开场结束后再接管浏览器时钟，避免把启动遮罩冻结。

## 验证范围

`e2e/auto-demo.e2e.ts` 使用真实 Chrome，覆盖 1440 × 1000 和 390 × 844；完成导图另检查 684 × 900、1280 × 720。

完整流程通过应用原有控件执行导入、维护、整改、复核、报表、匹配、备份及重置。验证 11 个演示文件，读取 JSON 与 Excel 内容，确认培训导出只包含一名已选人员、A 公司、当前岗位检修工、目标岗位储能运维工程师及指定评估日期。退出或重播后，原有数据、页面和表格密度恢复。

新增知识图谱断言覆盖人员工号、有效证书关系、人员详情跳转、证书到规则追溯、规则页面跳转、无结果和全景恢复；手机端检查讲解卡片与当前持证记录、全景画布互不覆盖。桌面暂停图谱动效，手机遵从减少动态效果设置。

新增人才筛选回归验证：两项证书交集得到 6 人，再选低压电工作业证出现空结果；键盘取消该项后恢复 6 人；未来评估日期使过期证书失效，恢复日期后结果恢复。清空证书、取消岗位选择、Escape 回焦及窄屏无整页横向溢出均有断言。

完整演示回归使用受控浏览器时钟加速，并在截图与断言期间冻结；指针、点击落点、字幕、停留、暂停和逐字输入另外以标准倍速验证。打印仍是页面内报表版式预览，不代表系统打印对话框验收。

## 检查结果

- `npm test`：67 项通过（含最新知识图谱模型测试）。
- `npm run test:demo`：9 项通过，1 项按预期跳过（手机端不显示桌面密度按钮）。
- `npm run verify:data`：1,315 条岗位来源逐字段一致，124 处班组空白、4 处岗位空白保留。
- `npm run build`、`npm run verify:pages`、`npm run verify:ui`、`npm run format:check`：通过。
- `designmd lint DESIGN.md`：0 errors、0 warnings；strict premium audit：0 findings。
- 浏览器运行错误：完整演示检查结果为空。

## 相关证据

- [桌面人才筛选](inline-demo/desktop-talent-filters.png)
- [手机人才筛选](inline-demo/mobile-talent-filters.png)
- [桌面目标岗位与培训](inline-demo/desktop-talent-training.png)
- [手机目标岗位与培训](inline-demo/mobile-talent-training.png)
- [全流程导图](inline-demo/desktop-flow-map.png)

- [桌面人员关联](inline-demo/desktop-knowledge-person.png)
- [手机人员关联](inline-demo/mobile-knowledge-person.png)
- [桌面关系全景](inline-demo/desktop-knowledge-overview.png)
- [手机关系全景](inline-demo/mobile-knowledge-overview.png)
