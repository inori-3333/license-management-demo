# 知识图谱验收记录

2026-09-22，新增 `/knowledge-graph` 页面及桌面、手机导航入口。只读当前台账、证书目录、规则及统计日期的评估结果，沿用原业务口径。

## 实际浏览器检查

本地 Vite 页面，Codex 浏览器，1440 × 1000 与 390 × 844。

- 全景六类圆形聚类、浮动和流动连线；桌面 48 个节点、手机 24 个节点，全部 2307 个节点可搜索。
- 点击 C 公司展示 434 个直接关联节点；展开局部后中心定位，关联列表可从 20 项加载到 40 项。
- 搜索“高压电工作业证”获得 4 个结果，限定证书类型后 1 个结果；可查看有效、已过期、复审逾期的持证关系。
- 不存在的关键词显示无结果与清除入口；清空搜索后恢复详情。
- 暂停动效前后节点坐标保持不变；暂停期间拖拽和方向键移动仍有效，Enter 可选中节点。
- 缩放按钮由 100% 变为 120%，回到全景恢复初始视图。
- 手机搜索结果位于画布上方、详情在下方；页面文档宽度与视口均为 390px，无横向溢出。
- 浏览器检查期间无 console error。系统 reduced-motion 通过 CSS 和媒体查询实现，本轮未切换操作系统偏好做实机验证。

## 自动校验

`npm test`：67 项通过（含 6 项图谱关系测试）；`npm run build`、`verify:data`、`verify:ui`、`format:check`、`verify:pages` 均通过。图谱测试覆盖当前任职、证书别名及过期状态、同名岗位隔离、关系端点完整性、有限画布与空数据。

前端设计严格静态审计无发现，结果在忽略目录 `evidence/knowledge-graph-premium-audit.json`。上述证据属于本地实现与浏览器验证，不代表远端已发布。
